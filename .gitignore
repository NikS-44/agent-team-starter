node_modules
dist
.DS_Store
*.log
.dev-server.pid
.dev-server.log
.env
.env.local
# Local DB or app data at repo root only (not src/data/)
/data/
.claude/worktrees/
# Claude Code machine-only prefs (paths, permissions); not shared — see .ai-rules/README.md
.claude/settings.local.json
playwright-report/
test-results/
*.tsbuildinfo
/coverage/
# Generated by `scripts/patch-coverage` when run locally
/patch-coverage-comment.md
.fallow/
.cursor/mcp-memory.jsonl

# Verification screenshots and artifacts (local only; keep folder with .gitkeep)
verification/*
!verification/.gitkeep

# SQLite (local dev DB + WAL mode sidecars — regenerate via migrate/seed, do not commit)
*.sqlite
*.sqlite-shm
*.sqlite-wal
.worktrees/
